// Family Trivia Fluent app definition. Empty to start; tables, roles, and
// records are added in later tasks (see .superpowers/sdd plan) following the
// idioms in EmailOS/fluent/src/fluent/*.now.ts.
export {}
